%{
#include "y.tab.h"
extern int yylval;

%}
%%
\n { 
  // yytext는 렉서가 인식한 토큰을 저장하는 변수이다.
  // 렉서가 인식하지 않는 문자들을 모두 리터럴 토큰으로 취급하여 반환하는 기법이 흔히 사용된다.
  // 어차피 반환해도 yacc에 의해 처리되기 때문에, 렉서에서는 토큰을 구분하는 역할만 하면 된다.

  // 주석을 위해 임의의 토큰을 정의하였다. ↓
}
\. return yytext[0];
%%
int yywrap() { return 1; }
